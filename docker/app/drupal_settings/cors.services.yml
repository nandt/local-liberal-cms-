# Configure Cross-Site HTTP requests (CORS).
# Read https://developer.mozilla.org/en-US/docs/Web/HTTP/Access_control_CORS
# for more information about the topic in general.
# Note: By default the configuration is disabled.
parameters:
  cors.config:
    enabled: ${ENABLE_CORS}
    # Specify allowed headers, like 'x-allowed-header'.
    allowedHeaders: ['AMP-Same-Origin','content-type','accept','origin','x-requested-with', 'access-control-allow-origin', 'x-csrf-token', 'authorization', 'content-disposition']
    # Specify allowed request methods, specify ['*'] to allow all possible ones.
    allowedMethods: ['OPTIONS','GET','POST','PATCH','DELETE']
    # Configure requests allowed from specific origins.
    allowedOrigins: ['https://www-liberal-gr.cdn.ampproject.org', 'http://localhost:3000', '${CORS_DASHBOARD_ORIGIN}']
    # Sets the Access-Control-Expose-Headers header.
    exposedHeaders: false
    # Sets the Access-Control-Max-Age header.
    maxAge: false
    # Sets the Access-Control-Allow-Credentials header.
    supportsCredentials: true